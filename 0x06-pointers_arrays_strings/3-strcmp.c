#include "main.h"
#include <string.h>

/**
 * _strcmp -> function for comparing purpose
 * @s1: first param
 * @s2: second param
 * Return: integer
 */
int _strcmp(char *s1, char *s2)
{
	int result;

        result = strcmp(s1, s2);
                return (result);
}
